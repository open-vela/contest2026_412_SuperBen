submission: 补齐应用层与板级源码，重生成权威补丁

将评审此前无法在仓内定位的作品源码补入本仓，并按官方目录约定组织：

- app/chatui/：微信式聊天界面（LVGL 应用，chatui.c + 构建文件）
- board/contest_board/：ESP32-P4 板级支持包完整源码
  （bringup / ek79007_panel / touch / es8311 / ethernet / gpio / 按钮 / 复位 / 启动，
  含 configs/ai_agent/defconfig 与 configs/nsh/defconfig、include/board.h、
  scripts/Make.defs、Kconfig、CMakeLists）
- overlay/：公共仓（nuttx / packages / apps）改动的最终完整文件，供评审直接阅读
- tools/apply_overlay.sh：把 overlay/ 一键应用到已同步的工作树

patches/ 重新生成为与 overlay 同源的权威 unified diff：

- nuttx-esp32p4-port.patch（23 文件：task_init NULL-attr、nuttx_add_library SYSTEM、
  es8311 ENOTTY 三元 bug、sa8339、gt9xx、ft80x、aht10、bmp180、esp32p4 芯片层等）
- packages-ai_agent.patch（16 文件：mimo_asr/mimo_tts 后端、agent_loop、voice_channel、
  memory_store、audio_playback、vela_tls 等）
- apps-chatui.patch（chatui 5 文件 + examples/Kconfig）
- 保留 esp32p4_i2c_clock_timeout_fix.patch（两处 I2C bug：|| → |、clock_t 无符号超时）

移除官方脚手架占位（app/hello_app、quickapp/hello_quickapp、board_boot.c）
与已并入上述补丁的旧残缺补丁；manifest 的 linkfile 映射改为实际目标路径；
README 重写为与本仓实际内容一致（含目录结构、overlay/patches 双轨说明、
功能状态如实标注、AI 日志说明）。
